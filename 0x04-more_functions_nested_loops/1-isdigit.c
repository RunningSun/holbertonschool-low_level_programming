#include "holberton.h"

/**
 * _isdigit - check for digit 0 to 9
 * @c: an int passed by main function
 *
 * Description: Uses headers to link function
 * Return: 0 or 1
 */

int _isupper(int c)
{
	if (c >= 48 && c <= 57)
	return (1);
return (0);
}
